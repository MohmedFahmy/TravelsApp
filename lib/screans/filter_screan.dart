import 'package:flutter/material.dart';

class FilterScrean extends StatelessWidget {
  const FilterScrean({super.key});
  static const ScreanRoute = '/filter';

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('data'),
    );
  }
}
